import assert from'node:assert/strict';import{createRequire}from'node:module';import fs from'node:fs';const require=createRequire(import.meta.url),A=require('../functions/lib/assurance-controls.js');
const good=A.evaluate({movements:{sale:{type:'order_sale',sourceType:'order',sourceId:'o1',lines:[{debit:100},{credit:100}]}},audits:{a:{action:'sale'}},approvals:{p:{usedAt:1,usedBy:'sale_o1'}},admins:{u:'manager'},permissions:{u:{finance:true}}});assert.equal(good.status,'operator_signoff_required');assert.equal(good.readOnly,true);assert.equal(good.counts.blocked,0);
const bad=A.evaluate({movements:{bad:{lines:[{debit:100}]}},permissions:{orphan:{finance:true}}});assert.equal(bad.status,'blocked');assert.ok(bad.counts.blocked>=3);
const source=fs.readFileSync(new URL('../src/functions/20-portal-auth.js',import.meta.url),'utf8');for(const marker of ['AssuranceControls.evaluate','/financialMovements").limitToLast(100)','/operationalAudit").limitToLast(100)','/financialApprovals").limitToLast(100)'])assert.ok(source.includes(marker),`Phase 19 safeguard missing: ${marker}`);console.log('PASS: Phase 19 performs bounded, read-only access, journal, source, reversal, approval, and audit assurance.');
