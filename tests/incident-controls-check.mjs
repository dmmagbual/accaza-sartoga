import assert from'node:assert/strict';import{createRequire}from'node:module';import fs from'node:fs';const require=createRequire(import.meta.url),C=require('../functions/lib/incident-controls.js'),actor={uid:'manager-a',role:'manager'},now=1788105600000;
const row=C.normalizeCreate({severity:'SEV2',title:'Online checkout unavailable',impact:'Customers cannot place online orders',financialImpact:true},actor,now);assert.equal(row.status,'investigating');assert.equal(row.financialImpact,true);assert.equal(C.nextStatus('investigating','identified'),'identified');assert.throws(()=>C.nextStatus('investigating','resolved'));assert.throws(()=>C.resolutionEvidence({evidence:{serviceVerified:true}}));assert.equal(C.resolutionEvidence({evidence:{serviceVerified:true,backupVerified:true,inventoryReconciled:true,financeReconciled:true},summary:'All controls balanced'}).financeReconciled,true);
const source=fs.readFileSync(new URL('../src/functions/20-portal-auth.js',import.meta.url),'utf8');for(const marker of ['exports.manageIncident = onCall','incidentCommandClaims','current?undefined','different management reviewer','Incident evidence only; no order, stock, subledger, Finance movement, or Books journal changed.'])assert.ok(source.includes(marker),`incident safeguard missing: ${marker}`);console.log('PASS: Phase 14 incidents enforce severity, lifecycle, reconciliation evidence, reviewer separation, idempotency, and audit-only writes.');
