rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Phase 1 is a server-managed replica. Browser access stays closed so the
    // POS cannot accidentally depend on Firestore before cutover verification.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
