import{firebaseConfig,callables,initializeApp,deleteApp,getAuth,signInWithEmailAndPassword,setPersistence,inMemoryPersistence}from"./firebase-client.mjs";

function requestManagerApproval(action,sourceId,amount,reason){return new Promise(function(resolve,reject){
  var mask=document.createElement('div');mask.style.cssText='position:fixed;inset:0;background:rgba(0,0,0,.58);z-index:12000;display:flex;align-items:center;justify-content:center;padding:1rem;';
  mask.innerHTML='<div style="background:#fff;border-radius:12px;max-width:430px;width:100%;padding:1.2rem;box-shadow:0 12px 40px rgba(0,0,0,.25);"><div style="font-weight:700;color:var(--bd);font-size:1rem;">🔐 Manager approval</div><p style="font-size:.78rem;color:var(--tl);line-height:1.45;">A manager must sign in with their own Firebase portal account. The cashier session will remain open.</p><label class="pz-lbl">Manager email</label><input class="pz-in" id="maEmail" type="email" autocomplete="username"/><label class="pz-lbl" style="margin-top:.5rem;">Manager password</label><input class="pz-in" id="maPass" type="password" autocomplete="current-password"/><div id="maErr" style="display:none;color:#b42318;font-size:.75rem;margin-top:.5rem;"></div><div style="display:flex;gap:.5rem;margin-top:1rem;"><button class="pz-btn ok" id="maOk">Approve</button><button class="pz-btn sec" id="maCancel">Cancel</button></div></div>';
  document.body.appendChild(mask);function close(){if(mask.parentNode)mask.parentNode.removeChild(mask);}
  mask.querySelector('#maCancel').onclick=function(){close();reject(new Error('Manager approval cancelled.'));};
  mask.querySelector('#maOk').onclick=async function(){var btn=this,email=(mask.querySelector('#maEmail').value||'').trim(),password=mask.querySelector('#maPass').value||'',err=mask.querySelector('#maErr');if(!email||!password){err.textContent='Enter the manager email and password.';err.style.display='block';return;}btn.disabled=true;btn.textContent='Verifying…';var mgrApp=null;try{mgrApp=initializeApp(firebaseConfig,'manager-approval-'+Date.now()+'-'+Math.random().toString(36).slice(2));var mgrAuth=getAuth(mgrApp);await setPersistence(mgrAuth,inMemoryPersistence);var cred=await signInWithEmailAndPassword(mgrAuth,email,password),token=await cred.user.getIdToken(true),res=await callables.createManagerApproval({action:action,sourceId:String(sourceId),amount:amount==null?null:Number(amount),reason:String(reason||''),managerIdToken:token});close();resolve((res&&res.data)||res);}catch(e){err.textContent='Approval failed: '+((e&&e.message)||(e&&e.code)||e);err.style.display='block';btn.disabled=false;btn.textContent='Approve';}finally{if(mgrApp)try{await deleteApp(mgrApp);}catch(_e){}}};
  setTimeout(function(){var e=mask.querySelector('#maEmail');if(e)e.focus();},50);
});}

export{requestManagerApproval};
